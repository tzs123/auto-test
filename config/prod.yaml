# 生产环境配置（由 utils/yaml_loader.load_config 读取，ENV=prod 时生效）
base_url: http://www.example.com:9527
timeout: 30
headers:
  Content-Type: application/json
# JDY 接口签名 access_key（按实际填写）
access_key: "PROD_ACCESS_KEY_HERE"
